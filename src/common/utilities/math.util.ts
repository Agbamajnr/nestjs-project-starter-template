export function calculatePercentage(number: number, perc: number): number {
    return (number / 100) * perc;
}

export function calculateNumberPercentage(n1: number, n2: number): number {
    return (n1 / n2) * 100;
}